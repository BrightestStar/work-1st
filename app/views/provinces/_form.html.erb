<%= simple_form_for @province do |f| %>

<div class="group">
  <%= f.input :name %>
</div>
<div class="group">
  <%= f.input :city %>
</div>
<div class="group">

<%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
